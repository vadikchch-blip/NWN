# Cloudflare R2 Configuration
# Get these values from your Cloudflare dashboard:
# Dashboard > R2 > Manage R2 API Tokens

# Your Cloudflare Account ID (found in the URL when logged into dashboard)
R2_ACCOUNT_ID=your_account_id_here

# R2 API Token - Access Key ID
R2_ACCESS_KEY_ID=your_access_key_id_here

# R2 API Token - Secret Access Key
R2_SECRET_ACCESS_KEY=your_secret_access_key_here

# R2 Bucket Name (must be private bucket)
R2_BUCKET_NAME=podcasts

# Server Configuration
PORT=3000

# URL expiration time in seconds (5-10 minutes recommended)
# 300 = 5 minutes, 600 = 10 minutes
URL_EXPIRATION_SECONDS=600

# Allowed origins for CORS (comma-separated)
# Leave empty or set to * for development
# In production, set to your domain: https://yourdomain.com
ALLOWED_ORIGINS=

# ── Supreme First Access ──
# PostgreSQL (Railway)
DATABASE_URL=postgresql://user:pass@host:port/dbname
DATABASE_PUBLIC_URL=postgresql://user:pass@host:port/dbname

# First Access photos (R2 public bucket)
# URL: ${R2_PUBLIC_BASE_URL}/Supreme/${image_key}.webp
R2_PUBLIC_BASE_URL=https://pub-fa833a523d7b4426930443e4050356ce.r2.dev
# Optional: R2_FIRST_ACCESS_PATH=Supreme  R2_FIRST_ACCESS_EXT=.webp

# Если xlsx не в data/, укажи URL (GitHub raw или другой):
# SUPREME_XLSX_URL=https://raw.githubusercontent.com/user/repo/main/data/ррц%20Supreme.xlsx

# Max active reservations per invite (default 5)
MAX_ACTIVE_RESERVATIONS_PER_INVITE=5

# Reservation TTL in hours (default 24), если не задано RESERVATION_FIXED_EXPIRES_AT
RESERVATION_TTL_HOURS=24

# Фиксированная дата окончания всех броней (ISO, UTC). Если задано — все новые брони истекают в этот момент. 5 марта 20:00 МСК = 17:00 UTC:
RESERVATION_FIXED_EXPIRES_AT=2026-03-05T17:00:00.000Z
